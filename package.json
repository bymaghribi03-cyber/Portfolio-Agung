{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
  "build": "vite build"
}
  "preinstall": "...",
  "build": "pnpm run typecheck && pnpm -r --if-present run build",
  "build:portfolio": "pnpm --filter @workspace/portfolio run build",
  "deploy": "pnpm run build:portfolio && pnpm --filter @workspace/portfolio run deploy",
  "cf:deploy": "wrangler deploy --assets ./portfolio/dist",
  "typecheck:libs": "tsc --build",
  "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
